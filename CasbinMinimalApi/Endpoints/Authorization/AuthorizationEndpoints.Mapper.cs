namespace CasbinMinimalApi.Endpoints.Authorization;

// Authorization endpoints currently return primitive/record responses; no mapping logic required.
// Add mapper helpers here if transformation becomes more complex.

